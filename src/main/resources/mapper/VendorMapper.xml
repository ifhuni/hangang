<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hangang.web.vendor.VendorMapper">

    <select id="existsByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM vendor
        WHERE email = #{email}
    </select>

    <select id="findByEmail" parameterType="string" resultType="com.hangang.web.vendor.Vendor">
        SELECT vendor_id, business_name, business_reg_no, ceo_name, phone, email, password, status, created_at, approved_at
        FROM vendor
        WHERE email = #{email}
    </select>

    <insert id="insertVendor" parameterType="com.hangang.web.vendor.Vendor"
            useGeneratedKeys="true" keyProperty="vendorId">
        INSERT INTO vendor (business_name, business_reg_no, ceo_name, phone, email, password, status)
        VALUES (#{businessName}, #{businessRegNo}, #{ceoName}, #{phone}, #{email}, #{password}, 'PENDING')
    </insert>

    <select id="findByStatus" parameterType="string" resultType="com.hangang.web.vendor.Vendor">
        SELECT vendor_id, business_name, business_reg_no, ceo_name, phone, email, password, status, created_at, approved_at
        FROM vendor
        WHERE status = #{status}
        ORDER BY created_at
    </select>

    <update id="approve" parameterType="long">
        UPDATE vendor
        SET status = 'APPROVED', approved_at = CURRENT_TIMESTAMP
        WHERE vendor_id = #{vendorId} AND status = 'PENDING'
    </update>

    <update id="reject" parameterType="long">
        UPDATE vendor
        SET status = 'REJECTED'
        WHERE vendor_id = #{vendorId} AND status = 'PENDING'
    </update>

</mapper>